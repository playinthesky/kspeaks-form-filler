schema_version: "0.1.0"
form_id: staff_profile
ko_name: 직원정보
title: Staff profile
status: designed_skeleton_committed

source:
  canonical_filename: source.hwpx
  path: templates/forms/staff_profile/source.hwpx
  storage: private_data_repo
  allowed_locations:
    - kspeaks-form-data
  commit_to_public_git: false

build:
  skeleton_hwpx: skeleton/skeleton.hwpx
  mapping: mapping.json
  typography_profile: default_160
  commit_to_public_git:
    skeleton_hwpx: true
    mapping: true

checks:
  phase_1_output_format: hwpx
  pdf_preview: excluded
  preserve_original_layout: true
  requires_roundtrip_validation: true
  design_pass: header_two_column_profile
